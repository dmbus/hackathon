services:
  backend:
    build: .
    image: ${IMAGE_NAME:-backend}
    expose:
      - "8000"
    env_file:
      - .env
    restart: always
    networks:
      - default
      - nginx-proxy

networks:
  nginx-proxy:
    external: true
    name: nginx-proxy_default
